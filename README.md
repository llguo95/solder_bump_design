This is the repository that accompanies the article entitled "Gaussian Process Regression Modeling Benchmark of Atomic Vacancy Concentrations in Solder Bumps".

This repository use the `uv` package manager. To run any functionality within the repository, first execute `uv sync` to generate a virtual environment before executing, e.g., `uv run gpr_compare.py`.